# 简化的ALSA配置 - 避免复杂设备引用错误
pcm.!default {
    type pulse
}

ctl.!default {
    type pulse
}

# 备用配置 - 如果PulseAudio不可用
pcm.backup {
    type plug
    slave {
        pcm "hw:1,0"
        rate 16000
        channels 1
        format S16_LE
    }
}