# mcar Release Gate Report (2026-02-17)

- Scope: Two-phase release gate plan (Gate P/A/B/C)
- Executor: Codex
- Environment: local development machine (non-Raspberry Pi)
- Decision: **NO-GO**

## Gate P — Preparation & Baseline

### Results
- PASS: Node runtime present (`v19.8.1`, satisfies >=18)
- FAIL: Python runtime is `3.10.9` (requires `>=3.11` for `modules`)
- PASS: `.ai_pet_env` file exists
- FAIL: `GEMINI_API_KEY` missing in `.ai_pet_env`
- N/A-KEY-MISSING: `PICOVOICE_ACCESS_KEY` missing (wake-word tests can be skipped)
- N/A-DATA-MISSING: `/Users/xiaorain/code/mcar/data/` not found, so no backup artifact generated

## Gate A — Local Automated Validation

### Core (`/Users/xiaorain/code/mcar/core`)
- FAIL: `npm install`
  - blocker: network/DNS access to npm registry failed (`ENOTFOUND registry.npmjs.org`)
- FAIL: `npm run build` (`tsc: command not found`)
- FAIL: `npm test` (`vitest: command not found`)
- FAIL: `npm run test:coverage` (`vitest: command not found`)

### Modules (`/Users/xiaorain/code/mcar/modules`)
- FAIL: `pip3 install -e ".[dev]"`
  - blocker: editable install unsupported in current Python/pip toolchain for this `pyproject.toml`
- FAIL: `pytest -q`
  - blocker: dependency missing (`ModuleNotFoundError: No module named 'zmq'`)
  - collection stopped with 7 import errors

## Gate B — Local Service/API Contract Validation

- BLOCKED: prerequisites from Gate A not met (core dependencies/build unavailable)
- Not executed: runtime API checks for `/api/status`, `/api/invoke`, `/api/chat`, `/api/mode`, watchdog stability

## Gate C — Raspberry Pi Full Validation

- NOT EXECUTED in current environment
- Reason: requires real hardware path (GPIO/I2C/audio/button/motion sensors) on Raspberry Pi

## Contract Artifacts Implemented

- Added contract checklist: `/Users/xiaorain/code/mcar/docs/CONTRACT_CHECKLIST.md`
- Corrected test doc contract mismatches in `/Users/xiaorain/code/mcar/docs/TESTING.md`:
  - `duration` -> `duration_ms` for motion calls
  - `duration` -> `timeout_s` for `tool.voice.recognize`
  - infrared expected keys -> `left_obstacle/right_obstacle`
  - cooldown default text updated to 2 seconds

## Blocking Issues (Priority)

1. P0: Missing package registry access blocks `npm install`.
2. P0: Python version mismatch (`3.10` vs required `>=3.11`).
3. P0: Missing `GEMINI_API_KEY` blocks chat/LLM path validation.
4. P1: Python dependency/toolchain mismatch prevents module install and tests.

## Re-test Entry Criteria

1. Restore npm registry connectivity in this environment.
2. Use Python 3.11+ (virtualenv recommended).
3. Install module dependencies successfully (`pyzmq`, pytest plugins).
4. Provide valid `GEMINI_API_KEY` in `.ai_pet_env`.
5. Re-run Gate A completely before entering Gate B.
