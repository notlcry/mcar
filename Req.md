---

# 树莓派 Agent 交互机器人系统需求文档（PRD/RFC 混合）

## 0. 版本与状态

* 文档版本：1.0
* 状态：需求基线（可迭代）
* 目标平台：Raspberry Pi（首选 Pi 4B）+ Linux（首选 Raspberry Pi OS 64-bit）
* 推理方式：外部大模型 API（例如 Gemini）

---

## 1. 项目概述

### 1.1 项目目标

构建一个运行在树莓派上的交互机器人系统，使其具备：

1. **语音为主的人机交互**（听懂、对话、执行、反馈）
2. **Agent 编排能力**（理解意图、规划步骤、调用能力单元、处理异常）
3. **硬件可扩展能力**（接入新硬件后，自动形成可调用能力，供 Agent 使用）
4. **持久记忆能力**（跨会话的偏好/事实/设备画像/任务进度记忆，支持检索与撤销）
5. **安全与可靠**（可急停、可降级、可审计、避免失控与误操作）

### 1.2 非目标（明确不做或首期不强制）

* 不要求树莓派本地运行大模型（推理走 API）
* 不要求实现复杂导航/SLAM/路径规划
* 不要求构建云端统一管理平台（可后续）
* 不要求提供“技能市场”对公网发布（安全风险高，可后续）

---

## 2. 术语与定义

* **Agent**：能理解用户意图、规划并调用能力单元执行任务的决策主体。
* **能力单元（Capability Unit）**：可被 Agent 调用的功能抽象，分为：

  * **Tool**：小粒度、可参数化的操作（如 `set_led`、`read_temp`、`move`）
  * **Skill**：大粒度、封装流程的任务（如 `patrol`、`self_check`）
* **记忆（Memory）**：跨会话持久化的信息体系（结构化条目 + 检索 + 可控写入）。
* **设备记忆（Device Memory）**：与硬件相关的持久信息（校准、限制、健康、故障历史、能力清单）。
* **安全门禁（Safety Gate）**：对危险能力的硬约束（权限、确认、参数边界、频率限制等）。
* **扩展模块（Extension/Plugin）**：新增硬件或服务能力的封装单元，可被系统安装/启用/禁用。

---

## 3. 用户角色与需求

### 3.1 角色

1. **最终用户**：通过语音/文本/网页与机器人交互，发出命令并接收反馈。
2. **开发者/维护者**：接入硬件、编写工具/技能、调试与更新系统。
3. **管理员（可选）**：管理权限、模块白名单、日志与备份。

### 3.2 用户价值

* 用户：用自然语言指挥机器人完成动作与任务，获得可解释且安全的反馈。
* 开发者：硬件能力接入成本低，能力可复用、可组合、可测试。
* 管理员：可控的权限与审计，降低误操作与供应链风险。

---

## 4. 功能性需求

### 4.1 交互通道（I/O）

#### 4.1.1 语音交互（必须）

* 系统必须支持语音输入与语音输出。
* 系统必须支持两种交互触发方式（可配置）：

  1. **唤醒模式**：需唤醒词/按钮触发后开始听（降低误触发）
  2. **按键/网页触发模式**：适合开发调试/安静环境
* 系统必须支持以下语音交互语义：

  * 命令：如“前进一点”“开灯”“播报温度”
  * 控制：如“停”“取消”“继续”“重复一遍”
  * 模式：如“进入安全模式/儿童模式/静音”
* **停止词优先级**（必须）：识别到停止词（停/停止/别动/急停/stop 等）时，系统必须立即执行停止逻辑（见 6 章），且不依赖模型推理再判断。

#### 4.1.2 文本交互（必须，作为调试/兜底）

* 系统必须支持通过文本输入下达指令（CLI 或 Web），并获得等价的执行能力与反馈。
* 文本通道在语音不可用（设备故障/网络波动）时可用于诊断与恢复。

#### 4.1.3 Web/控制台（建议，非强制）

* 系统应提供可视化界面或控制台能力，用于：

  * 查看当前状态（模式、健康、最近动作、错误）
  * 查看能力清单（tools/skills）
  * 查看与管理记忆（查看/删除/导出）
  * 开发调试（日志、回放）

---

### 4.2 Agent 核心能力（必须）

#### 4.2.1 意图理解与规划

* Agent 必须能够将用户输入映射为：

  * 直接工具调用（单步）
  * 技能执行（多步、可复用流程）
  * 澄清提问（不明确时）
* Agent 必须支持：

  * **任务拆解**（将复杂任务拆成工具/技能序列）
  * **状态感知**（基于设备状态决定能否执行）
  * **异常处理**（工具失败时重试/降级/终止）

#### 4.2.2 工具/技能调用

* Agent 必须只通过已注册的能力单元执行外部动作。
* Agent 必须能够获取当前可用能力列表，并理解每个能力的参数、约束、风险级别。

#### 4.2.3 反馈与对话策略

* 系统必须提供执行反馈：

  * 执行前（可选：需确认时）
  * 执行后（必须：成功/失败/拒绝原因）
* 输出应简洁可控（避免长篇输出造成噪音），并支持“解释原因”模式（调试模式）。

---

### 4.3 能力扩展系统（必须，项目核心）

#### 4.3.1 能力单元模型（必须）

系统必须将所有可被 Agent 使用的能力抽象成统一模型，至少包含：

* `id`：唯一标识
* `name`：可读名称
* `description`：用途说明
* `type`：tool 或 skill
* `version`：版本标识
* `inputs`：参数定义（类型、范围、默认值、必填）
* `outputs`：返回结构定义
* `constraints`：约束（频率、时长、环境条件、资源占用）
* `risk_level`：风险级别（只读/普通/危险）
* `required_state`：所需状态条件（如 obstacle=false 才能移动）
* `permissions`：权限策略（谁能调用、是否需要确认）
* `observability`：是否必须记录审计事件、记录粒度要求

#### 4.3.2 扩展模块要求（必须）

* 系统必须支持以“模块/插件”的方式新增能力。
* 新模块必须可：

  * 安装/卸载（至少支持启用/禁用）
  * 声明依赖与权限（访问 GPIO/I2C/网络/文件等）
  * 提供健康检查（可选，但建议）
* 系统必须支持能力发现：新增模块后，Agent 可发现并使用其能力。

#### 4.3.3 能力组合（必须）

* 系统必须允许 Skill 由多个 Tool 组合而成（顺序/条件/重试/回滚语义不限定，但必须支持“组合执行”的表达能力）。
* Skill 必须可复用、可参数化（例如“巡逻 2 次”）。

---

### 4.4 记忆能力（必须，项目核心）

#### 4.4.1 记忆类型（必须）

系统必须支持至少三类记忆：

1. **会话记忆 Session Memory**

* 单次会话内的上下文（可摘要/压缩）
* 不要求跨重启持久化

2. **长期记忆 Long-term Memory**

* 跨会话、跨重启持久化
* 以结构化条目形式存储（见 4.4.2）

3. **设备记忆 Device Memory**

* 与硬件/环境相关的持久化信息：

  * 能力清单与约束
  * 校准/参数（如阈值、偏置）
  * 故障历史与恢复提示
  * 健康趋势数据摘要（可选）

#### 4.4.2 记忆条目最小信息模型（必须）

每条长期/设备记忆至少包含：

* `id`
* `type`：preference / fact / device / location / task / rule / incident / other
* `content`：结构化内容（可包含自然语言，但必须可字段化）
* `source`：user_explicit / user_implicit / system_detected / imported
* `confidence`：0~1 或等级（low/med/high）
* `created_at`, `updated_at`
* `ttl`（可选）
* `privacy_level`：private / normal / shareable
* `tags`（可选）

#### 4.4.3 记忆写入策略（必须）

系统必须支持两种写入模式，并可配置默认策略：

* **显式写入（用户确认）**：用户明确要求“记住/以后都这样”才写入；对敏感或高风险条目必须二次确认。
* **隐式写入（自动抽取）**：系统可提议写入（如“我可以记住你喜欢低音量吗？”），必须支持用户拒绝。

#### 4.4.4 记忆检索与注入（必须）

* 系统必须提供记忆检索能力（供 Agent 使用），至少支持：

  * 关键词检索
  * 语义相关检索
  * 按类型/隐私等级过滤
* 系统必须支持“上下文注入策略”：

  * 对每次用户请求，自动取回最相关的少量长期/设备记忆提供给 Agent
  * 注入必须受限：条数上限、敏感信息不自动注入或需授权

#### 4.4.5 记忆管理（必须）

用户必须可：

* 查询当前记忆概览（“我记住了什么？”）
* 删除/撤销某条记忆（“忘掉 X”）
* 清空某类记忆（仅偏好/仅会话/仅设备）
* 导出/备份/迁移记忆（格式不限定）

---

### 4.5 设备与环境状态（必须）

系统必须提供统一的“状态视图”供 Agent 与 UI 使用，至少包含：

* 运行模式（normal/safety/kid/debug/mute 等）
* 关键硬件健康（音频输入/输出是否可用、传感器是否在线）
* 最近一次动作与结果
* 风险状态（急停、障碍、温度过高、电量低等，具体项随硬件扩展）

---

## 5. 用例与验收（必须）

### 5.1 交互用例

1. **唤醒与回应**：用户唤醒后问“你能做什么？”系统列出若干能力示例（不超过 5 条）。
2. **澄清**：用户说“往那边一点”，系统问一个澄清问题而不是盲动。
3. **中断**：执行过程中用户说“停”，系统立即停止并确认。

### 5.2 能力用例

4. **工具调用**：用户说“把灯打开”，系统调用 `set_led(on)` 并反馈结果。
5. **技能执行**：用户说“进入晚安模式”，系统执行一串动作（关灯→静音→播报）。
6. **权限/确认**：用户说“快速前进 5 秒”（危险动作），系统要求确认或拒绝（按策略）。

### 5.3 记忆用例

7. **显式记忆**：用户说“记住我晚上十点以后静音”，系统确认写入，并在未来自动生效。
8. **撤销记忆**：用户说“忘掉刚才那个静音规则”，系统删除并确认。
9. **设备记忆联动**：新增一个传感器模块后，系统设备记忆出现该模块能力与约束；Agent 可使用它。

### 5.4 扩展验收（开发者）

10. 开发者新增一个硬件模块（例如温湿度传感器），仅通过添加模块并声明能力，即可被 Agent 发现与调用。
11. 模块禁用后，其能力不应再被 Agent 调用；相关调用应返回“能力不可用”的明确错误。

---

## 6. 安全、权限与可靠性（必须）

### 6.1 风险分级与策略（必须）

每个能力必须标注风险级别并遵循策略：

* **只读**：默认允许（仍需审计）
* **普通**：允许，但受参数边界与频率限制
* **危险**：必须至少满足一种：

  * 用户确认
  * 权限授权（管理员/白名单）
  * 安全门禁通过（状态条件满足）

### 6.2 急停与停止策略（必须）

* 系统必须提供至少一种“立即停止”机制（语音停止词/物理按钮/软件接口任一；强烈建议语音 + 物理双通道）。
* 停止必须具备最高优先级：一旦触发，必须优先中断当前任务/动作。

### 6.3 降级与容错（必须）

* 模型 API 不可用时：

  * 系统必须进入安全降级：拒绝危险能力，仅保留只读/基础回应，并告知用户原因。
* 单个模块故障：

  * 不应导致系统全局不可用；应隔离故障并记录。
* 网络抖动：

  * 系统应避免重复执行危险动作（需幂等或防重复策略，需求层面只要求“不会因为重试造成重复危险动作”）。

### 6.4 审计与可追溯（必须）

* 系统必须记录以下事件（结构化日志）：

  * 用户输入（语音转文本结果、置信度、时间）
  * Agent 决策（选择了哪些能力、是否澄清/确认）
  * 能力调用（参数摘要、结果、错误码）
  * 记忆写入/删除（来源、理由、确认记录）
  * 风险事件（急停、拒绝原因、异常）
* 日志必须可导出，用于回放与诊断。

---

## 7. 性能与体验（非功能需求）

* 语音交互延迟：应可监测、可配置，满足“可接受的交互体验”（不设硬阈值，但需可观测）。
* 系统可用性：设备重启后应自动恢复到可交互状态（自启动、健康检查）。
* 输出控制：默认语音输出应简短，避免噪音；支持静音模式。

---

## 8. 可维护性与可测试性（必须）

### 8.1 可测试性

* 系统应支持“无硬件测试模式”（mock 能力模块），用于回归测试与 CI。
* 关键安全策略必须可单元测试（例如危险能力必须确认、急停优先级等）。

### 8.2 可维护性

* 能力模块必须具备明确的版本与依赖声明。
* 需支持备份/恢复：至少包含记忆数据与关键配置。

---

## 9. 数据与隐私（必须）

* 必须支持对记忆进行隐私分级（private/normal/shareable）。
* 默认不应将敏感记忆自动注入模型上下文（需策略控制）。
* 对外部模型 API 的调用必须遵循最小化数据原则（只发送必要上下文）。

---

## 10. 范围与里程碑（建议）

### M1（最小可用）

* 语音/文本交互通路
* Agent 可调用 ≥ 3 个能力（至少含 1 个硬件能力）
* 记忆：显式写入 + 查询 + 删除
* 安全：停止词 + 危险能力确认/拒绝
* 审计日志

### M2（扩展与稳定）

* 扩展模块规范与模板完善
* 设备记忆（校准/健康/能力约束）联动
* 更完善的模式：安全/儿童/调试/静音
* 更完善的降级策略与回放工具

---

# 附录 A：能力模块接入验收清单（模板）

新增模块必须提供：

* 能力声明（id/name/version/inputs/outputs/risk/constraints）
* 权限与门禁策略说明
* 健康检查（建议）
* 日志/审计事件覆盖说明
* 失败场景与错误码列表
  -（可选）mock 实现/测试用例

---

# 附录 B：记忆治理规则（建议写进系统规则）

* 不自动记住敏感信息，除非用户明确要求并确认。
* 自动抽取记忆必须可撤销、可解释。
* 每条记忆必须可追溯来源与时间。

---

如果你愿意，我可以在这份需求之上再补两份“配套文档”（仍然不涉及具体实现）：

1. **能力风险分级表**（常见硬件能力的 risk_level、默认确认策略、参数上限建议）
2. **用例驱动验收脚本**（按场景列出输入/期望/审计点/降级点）

你更希望下一步先补哪一份？
